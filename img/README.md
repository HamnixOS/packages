Screenshots for the landing page.

Expected files (referenced by ../index.html):
  desktop.png    the desktop at rest — panel, clock, icons
  menu.png       the application menu open
  browser.png    hambrowse rendering a real page
  terminal.png   hamsh in the terminal
  files.png      the file manager
  installer.png  the graphical installer

Captured from the real framebuffer scanout of a running system, not from
a host-side render. Host renders misrepresent proportional text, so a
screenshot taken that way is not the same picture a user sees.
